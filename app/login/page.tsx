import LoginClient from "./LoginClient";

export const metadata = {
  title: "Max 로그인 · Kitt AI",
  description: "Claude Max 구독으로 진단 엔진을 인증합니다.",
};

export default function Page() {
  return <LoginClient />;
}
